@using Restaurant.DTOs
@using Restaurant.Data.Models
@model PaymentFormDto

<div class="row">
    <div class="col-sm-12 offset-lg-2 col-lg-8 offset-xl-3 col-xl-6">
        <h2 class="heading-margin text-center">@ViewBag.Title</h2>

        <form method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label asp-for="Amount"></label>
                <input asp-for="Amount" class="form-control" placeholder="Enter a title:  ">
                <span asp-validation-for="Amount" class="small text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Method"></label>
                <input asp-for="Method" class="form-control" placeholder="Insert an image: ">
                <span asp-validation-for="Method" class="small text-danger"></span>
            </div>



            <input class="btn btn-primary mt-3" type="submit" value="Save" />
        </form>
    </div>
</div>